pipeline {
    agent any

    stages { 
        stage ("start docker container") { 
            steps {
                echo " =============== start docker container ================="
                dir ('project_jenkins/') {
                    sh 'docker-composr up -d'
                }
            }
        }
    }
}
